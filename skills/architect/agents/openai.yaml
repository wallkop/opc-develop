interface:
  display_name: "architect"
  short_description: "Optionally gate a public-boundary or one-way technical decision"
  default_prompt: "Use $architect when the result card changes a public boundary or one-way architecture decision; consume any optional PRD, write only the necessary TD records/contracts/evidence plan, gate them, then return to build."
policy:
  allow_implicit_invocation: false
