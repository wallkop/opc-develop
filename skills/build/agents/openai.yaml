interface:
  display_name: "build"
  short_description: "Deliver one semantics-routed increment through a real core journey"
  default_prompt: "Use $build after demo, PRD, and testcase approval to implement the approved Core-Case and produce runner-derived evidence."
policy:
  allow_implicit_invocation: true
