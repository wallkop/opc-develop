interface:
  display_name: "build"
  short_description: "Contract, implement, locally deploy, and verify in one run"
  default_prompt: "Use $build to partition the approved design into gated contracts, implement them via TDD subagents with RED/GREEN evidence, apply local env changes, and run the full black-box regression to a gated acceptance sheet."
policy:
  allow_implicit_invocation: false
