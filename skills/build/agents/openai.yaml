interface:
  display_name: "build"
  short_description: "Deliver one budgeted increment through a real core journey"
  default_prompt: "Use $build to deliver this as one <=4-hour increment: write the one-page result card, prove the real core journey, implement runnable slices, and produce a fresh acceptance receipt."
policy:
  allow_implicit_invocation: true
