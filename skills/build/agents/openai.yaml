interface:
  display_name: "build"
  short_description: "Deliver a structurally deep increment through a real core journey"
  default_prompt: "Use $build only after naming an exact structural trigger and obtaining demo, PRD, and testcase approval; implement the approved Core-Case and produce runner-derived evidence."
policy:
  allow_implicit_invocation: true
