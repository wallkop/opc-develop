interface:
  display_name: "demo"
  short_description: "Make the idea experienceable before any PRD"
  default_prompt: "Use $demo to create and human-approve the mandatory experienceable prototype before PRD, testcase, and build."
policy:
  allow_implicit_invocation: false
