interface:
  display_name: "ship"
  short_description: "Deploy to the test environment and run test acceptance"
  default_prompt: "Use $ship to collect and gate the release manifest, deploy to the test environment, run automated regression there, and route the human's test-acceptance verdict; approval merges the branch to trunk."
policy:
  allow_implicit_invocation: false
