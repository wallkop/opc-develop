interface:
  display_name: "lite"
  short_description: "One localized non-semantic change with proportional checks"
  default_prompt: "Use $lite for this localized change without new E2E semantics, with focused checks and proportional real-entry evidence, without feature documents or subagents."
policy:
  allow_implicit_invocation: true
