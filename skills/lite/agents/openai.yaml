interface:
  display_name: "lite"
  short_description: "Default route for bounded changes through existing architecture"
  default_prompt: "Use $lite for this bounded change through existing architecture, with focused checks and matching risk protection; upgrade only if an exact Build trigger is proven."
policy:
  allow_implicit_invocation: true
