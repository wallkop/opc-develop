interface:
  display_name: "prd"
  short_description: "Optionally turn durable product decisions into a gated PRD"
  default_prompt: "Use $prd when durable product decisions need a contract: derive prd.md and black-box testcases.md from whichever approved requirement/demo/result-card inputs exist, gate product sign-off, then return to build by default."
policy:
  allow_implicit_invocation: false
